# R3R Implementation Note

Rollback baseline: `codex/v0-r3b-named-cast-priority` exact `0ff2b3a215759112380d870833b90e3dccb26906`.

R3R deliberately replaces accumulated Writer wording with a shorter priority hierarchy. It keeps the R2C–R3B factual/player-authority boundaries while making focal causality explicit: the world acts independently, but the current PC action controls the narrative camera when it creates the strongest local cause.

Additional experimental tools in the same branch:

- `admin ...` / `admin: ...` renders an immediate noncanonical scene preview without changing saved continuity/history/relationships;
- each normal or admin scene block ends with a compact overlapping-squares copy control that copies that scene only.

This branch is disposable. Do not merge on CI alone; sustained human original-feel acceptance is mandatory.
